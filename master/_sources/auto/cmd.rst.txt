Command Line
=========================

docky
-----

Usage:
    docky [SWITCHES] [SUBCOMMAND [SWITCHES]] args...

Meta-switches:
    --dry-run                Dry run mode
    -h, --help               Prints this help message and quits
    --help-all               Print help messages of all subcommands and quit
    -v, --version            Prints the program's version and quits
    --verbose                Verbose mode

Switches:
    -e, --env VALUE      Environment flag

============== ==========
Subcommands:
============== ==========
    build                    Build or rebuild services
    deploy                   Deploy your application
    down                     Stop all services
    kill                     Kill all running container of the project
    logs                     View output from containers
    migrate                  Migrate your odoo project  First you need to
                             checkout the docky-upgrade template available here
                             : https://github.com/akretion/docky-upgrade (It's a
                             template a docky but based on open-upgrade'  Then
                             go inside the repository clonned and launch the
                             migration  * For migrating from 6.1 to 8.0 run:
                             docky migrate -b 7.0,8.0 * For migrating from 6.1
                             to 9.0 run: docky migrate -b 7.0,8.0,9.0 * For
                             migrating and loading a database run: docky migrate
                             -b 7.0,8.0 --db-file=tomigrate.dump
    new                      Create a new project
    open                     Open a new session inside your dev container
    ps                       List containers
    pull                     Pulls service images
    run                      Start services and enter in your dev container
                             After running the command you will be inside the
                             container and you will have access to the ak cmd
                             (see ak documenation) main command are 'ak run' and
                             'ak build'  Note: the container is accessible with
                             the following url : http://my_project.vd and
                             http://my_plugin.my_project.vd
    up                       Start all services in detached mode
============== ==========

build
-----

Build or rebuild services

Usage:
    docky build [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



deploy
------

Deploy your application

Usage:
    docky deploy [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



down
----

Stop all services

Usage:
    docky down [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



kill
----

Kill all running container of the project

Usage:
    docky kill [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



logs
----

View output from containers

Usage:
    docky logs [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



migrate
-------

Migrate your odoo project

First you need to checkout the docky-upgrade template
available here : https://github.com/akretion/docky-upgrade
(It's a template a docky but based on open-upgrade'

Then go inside the repository clonned and launch the migration

* For migrating from 6.1 to 8.0 run:
    docky migrate -b 7.0,8.0
* For migrating from 6.1 to 9.0 run:
    docky migrate -b 7.0,8.0,9.0
* For migrating and loading a database run:
    docky migrate -b 7.0,8.0 --db-file=tomigrate.dump

Usage:
    docky migrate [SWITCHES] args...

Hidden-switches:
    -h, --help                  Prints this help message and quits

Switches:
    -b, --branch VALUE      Branch to apply split by comma ex: 7.0,8.0;
                                required
    --db-file VALUE         Sets an attribute



new
---

Create a new project

Usage:
    docky new [SWITCHES] name

Hidden-switches:
    -h, --help      Prints this help message and quits



open
----

Open a new session inside your dev container

Usage:
    docky open [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



ps
--

List containers

Usage:
    docky ps [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



pull
----

Pulls service images

Usage:
    docky pull [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



run
---

Start services and enter in your dev container

After running the command you will be inside the container and
you will have access to the ak cmd (see ak documenation)
main command are 'ak run' and 'ak build'

Note: the container is accessible with the following url :
http://my_project.vd and http://my_plugin.my_project.vd

Usage:
    docky run [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits



up
--

Start all services in detached mode

Usage:
    docky up [SWITCHES] args...

Hidden-switches:
    -h, --help      Prints this help message and quits

